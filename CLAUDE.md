# CLAUDE.md

## リポジトリ概要

作業中に見えるダッシュボードを描画するデスクトップアプリ(Wails v2 / Go + Next.js)。[genact](https://github.com/svenstaro/genact) が流す擬似的な作業ログに、コミットグラフ・折れ線グラフ・グラフビュー・タコメータ風インジケータ・3D 散布図を加えて 1 画面に並べる。

- SDD 成果物: `docs/specs/NNN-<roadmap 名>/NNN-<unit>/`(flow-sdd が生成)

## 言語規約

- 会話・ドキュメント・コード内コメント・PR/Issue 本文・コミットメッセージは日本語で記述しなければならない(MUST)。
- 技術用語(Wails・WebView・WebGL 等の業界標準語)は英語のまま使用してよい。
- ライブラリ・関数・変数・ファイルパスなどの識別子は当該エコシステムの慣習に従う(通常は英語)。
- 仕様書・テスト項目・開発者向けのエラーメッセージの自然言語部分は日本語にする。
- 画面に描画する擬似ログとラベルは英語にする。模倣対象のツール(コンパイラ・パッケージマネージャ・CI 等)が英語で出力するため。

## 開発コマンド

```bash
# 開発モード(ホットリロード)
wails dev

# 配布用ビルド
wails build

# Go の静的検査とテスト
go vet ./...
go test ./...

# フロントエンドの lint と format
cd frontend && npm run lint
cd frontend && npm run format
```

## 技術スタック

- Wails v2 / Go 1.25(バックエンド。擬似データの生成とフロントエンドへの供給)
- Next.js 16 / React 19 / TypeScript(フロントエンド。静的エクスポートを Wails が embed する)
- Tailwind CSS 4(スタイル)
- Biome(フロントエンドの lint / format)

## Go コーディング規約

- エラーは戻り値で返す。`panic` はプログラマの誤りに限る
- 長時間動く処理には `context.Context` を第 1 引数で渡し、キャンセルで停止できるようにする
- ゴルーチンは終了条件を明示し、アプリ終了時に停止させる
- インターフェースは実装側ではなく利用側のパッケージで定義する
- 公開 API には godoc コメントを書く
- YAGNI: 将来の拡張性のためだけにコードを複雑化しない。過度な抽象化を避ける

## TypeScript コーディング規約

- `any` を使わない。型が定まらない箇所は `unknown` で受けて絞り込む
- 描画コンポーネントと擬似データの生成を別ファイルに分ける
- 再描画のたびに新しい関数・オブジェクトを作らない(アニメーションの発火頻度が高いため)
- Wails が生成するバインディング(`frontend/wailsjs/`)は手で編集しない

## コードコメント

- why / why not を書く。コードを読めば分かることは書かない

## 注意事項

- 実在のシステム情報を読み取らず、外部へも通信しない。画面に出る値はすべてこのアプリが生成した擬似データとする
- devcontainer は置かない。Wails のビルドに各 OS の WebView ツールチェーン(macOS では Xcode コマンドラインツール)が必要なため
- `frontend/dist` と `frontend/wailsjs` は `wails build` の生成物であり、リポジトリに含めない

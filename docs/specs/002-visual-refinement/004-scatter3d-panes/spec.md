# scatter3d-panes — 仕様

## 1. 目的と背景

roadmap `002-visual-refinement` の unit #4。unit #3(`scatter3d-pointcloud`)の完了時点で、3D 散布図は 6,000 点の地形・球・トーラスを高さに応じた色で描き、ドラッグで視点を変えられるが、点群は「原点を通る 3 軸と立方体の稜線 12 本」の上に浮いており、背景は `--color-surface-1` のべた塗りである。ユーザーは配布ビルドを中継役から渡された参考画像 3 枚(request.md §2.2 の参考サイトの画面。2026-09-06)と見比べ、残る差として「背面と底面のパネルと格子」「目盛りの数値と軸名」「点群の面への投影(影)」「むらのある深い青の背景」の 4 点を挙げた(roadmap §1 #4)。本 unit はこの 4 点を扱う。

参考画像から読み取った目標の具体は `research.md` §1 にある(画像はリポジトリに置かない。観察は 2026-09-07 に本セッションが行った)。要点は次のとおり。

- 縦の面 1 枚と床が立方体の面に貼り付き、視点と一緒に回る(本書は縦 4 面をすべて描き、奥を向く度合いで不透明度を連続に変えることで、見え方を参考の「奥の 1〜2 面」に揃える。§3 前提 2)。面の塗りはほぼ透明で、縁(1 px・不透明度 40〜50 %)と 3 × 3 の格子(1 px・不透明度 20〜35 %)で面が読める。
- 縦軸に目盛り 4 値(`2.08`・`1.35`・`0.62`・`-0.10`)と軸名 `Elevation`、床に目盛り 4 値(`1.08`・`0.36`・`-0.36`・`-1.09`)。文字は面に沿って歪み、鏡像にならない。本書の目盛りは参考の両端 `[-0.10, 2.08]` を 3 等分した値を `toFixed(2)` で書くため、2 番目が参考の `0.62` ではなく `0.63` になる(参考の刻みを逐語に再現しない)。
- 点群の投影が縦の面と床に、点の色を保ったまま暗く(不透明度 30〜45 %)写る。
- 背景は深い青の低周波のノイズで、視点を変えても動かない。

要求の本体は `docs/specs/002-visual-refinement/request.md`(§番号で参照)、unit の範囲と完了条件は `docs/specs/002-visual-refinement/roadmap.md` §1・§1.1 にある。凍結済みの `docs/specs/001-dashboard-mvp/002-scatter3d-panel/spec.md`(以下「凍結 spec」)、完了済みの `docs/specs/002-visual-refinement/002-scatter3d-controls/spec.md`(以下「#2 spec」)・`003-scatter3d-pointcloud/spec.md`(以下「#3 spec」)が定めた契約のうち、本書が置き換えるものは §3 前提 1 と §8 に列挙する。

## 2. スコープ

### 対象(やること)

- パネルの幾何(縦 4 面 + 床)と、ヨーから縦 4 面の「奥向きの度合い」(重み 0〜1 の連続量)を求める規則を、描画コンポーネントから切り出した純粋なモジュールとして定める(§5.2・§6.1)。unit #2 の `AXIS_SEGMENTS`(3 軸 + 稜線 12 本)をこのモジュールで置き換える
- 縦 4 面と床に、透明に近い塗り・3 × 3 の格子・縁を描く。縦面の不透明度は重みに比例させ、手前を向く面は透明(重み 0)になる(§6.3)
- 縦軸の目盛り 4 値と軸名 `Elevation` を、縦 4 面それぞれの右の縦の辺に、面と同じ重みの不透明度で、面に沿ったアフィン変換で貼る(§5.3・§6.2)。文字はマウント時にオフスクリーンへ描いた画像を毎フレーム `drawImage` する
- 点群を各パネルへ投影した影を、間引きと 8 段の色の表で描く(§5.4・§6.4)
- 背景を、マウント時に 1 度生成する低周波ノイズの画像(深い青 2 色の補間)に変える(§5.5・§6.5)
- 新しいトークン(背景 2 色・パネル 1 色・文字 1 色)を `globals.css` の `@theme` に足す(§6.6)
- TypeScript のテスト(面の重み・目盛りの値と書式・ラベルの変換・影の投影・ノイズの性質)と、`axes.test.ts` の置き換え(Requirement 1〜5)
- 完了時の p95・mean・max の計測と記録(Requirement 9)、配布ビルドの目視(Requirement 10)

### 対象外(やらないこと)

- ホイールによるズーム — 理由: request.md §4・roadmap §3 のスコープ外(承認済み)
- 原点を通る 3 軸の線と稜線 12 本 — 理由: 参考に無く、パネルの縁と格子が境界を示すため重複する(出典: 2026-09-06 のユーザーの決定「unit #2 で足した『原点を通る 3 軸』はこの unit で消す」。roadmap §1 #4 の指示ではない。この決定が roadmap 前提節の「`axes.ts` はそのまま使い、差し戻さない」を置き換える。§3 前提 1)
- 立方体の残りの稜線(手前側の縦の辺・上面の辺) — 理由: 参考に無い(`research.md` §1.1)。パネルの縁 3 面ぶんだけが線を持つ
- 画面に固定した直立の文字 — 理由: 参考と同じく面に沿って歪ませるとユーザーが決定した(roadmap §1 #4)
- 床の面を視点に応じて上面へ切り替えること — 理由: ピッチはドラッグを離すと 1.5 秒で既定へ戻り(#2 spec §5.2)、下から見上げる時間が短い。切り替えの規則を足すより、床を固定して点の下に描くほうが単純
- 影の奥行き順・影どうしの重なりの制御 — 理由: 影は 1 px の粒で不透明度が低く、重なりは色の濃さとして読める
- 背景のアニメーション — 理由: 参考の背景は視点を変えても動かない(`research.md` §1.5)。毎フレームの生成は割り当てと負荷を増やす
- 点群の生成(Go 側)・転送形式・`ScatterPoint` の契約の変更 — 理由: 目盛りの値は座標系のアフィン写像で定まり、Go 側から送る情報が無い(§3 前提 4)
- 依存グラフ・他パネル — 理由: unit #5 の範囲(roadmap §1)
- 計測器の改修 — 理由: roadmap §3

## 3. 前提(未検証の賭け)

1. **置き換える完了済みの契約は次のとおりで、これ以外は引き継ぐ。** #2 spec: §3 前提 1(3 軸 + 稜線 12 本を描く・格子を置かない・線の不透明度を奥行きで変える)→ §6.1・§6.3 / §2 対象外(ラベル)→ §5.3・§6.2 / §5.3(軸線の投影)→ §5.2 / §6.2(`AXIS_SEGMENTS`)→ §6.1 / Requirement 4.1〜4.7(軸線)→ Requirement 1・2・6。#2 spec §4 用語定義「ピッチ … 既定値は `SCATTER_PITCH`(-0.42)」→ §6.8。#3 spec: §5.2 事後条件「背景 → 軸線 → 点の順」→ §6.7 の描画順 / §5.3 `SCATTER_PITCH`「既存値 -0.42 のまま」→ §6.8 / §6.5 既存トークン「`--color-surface-1`(背景)・`--color-text-dim`・`--color-border`(軸線)は変えない」→ §6.6(散布図の背景と線をこれらから解決しなくなる。トークン自体は他パネルが使うため残す)/ Requirement 7.5「軸線を点より先に描き」→ Requirement 6.4 / Requirement 8.1「`axes.ts` を変更してはならない」→ Requirement 6.1 / Requirement 9.6(`fillStyle` 576 回以下・`fillRect` 点数以下)→ Requirement 9.5。roadmap 前提節「`axes.ts` はそのまま使い、差し戻さない」→ 2026-09-06 のユーザーの決定(3 軸と稜線 12 本を全廃する。§2 対象外)により置き換える(roadmap 本体は編集しない)。凍結済み・完了済みの文書は編集しない。 — 検証方法: Requirement 8 が引き継ぐ側を列挙し、`check.py` と文書ゲートで参照の実在を確かめる / 状態: 決定済み(roadmap §1 表の #4 行と前提節)
2. **パネルは「縦 4 面 + 床」とし、縦面の不透明度は「どれだけ奥を向いているか」の重み(0〜1 の連続量)に比例させる。** 参考は縦 1 面だが、自動回転(26 秒で 1 周)の下で 1 面を固定すると半周は面が手前に来て点群を覆い、文字が鏡像になる。ヨーから面を離散的に選ぶ案(最も奥の 1 面・奥向きの 2 面)は、透視投影(`FOCAL = 3.2`)では法線が真横を向く面も線に潰れず台形に見えるため、入れ替わりの瞬間に画が飛ぶ(文書ゲート 3 周目の指摘。`research.md` §2)。重みを法線の回転後 z 成分から連続に決めれば、離散的な選択が無く、飛びが原理的に起きない。手前を向く面は重み 0 で透明になり、見え方は参考と同じままになる。ラベルも面と同じ重みで各面に付け、面が濃くなるにつれて現れ、薄くなるにつれて消える(切り替わりの近くで 2 面ぶんのラベルが薄く重なる状態は許容する)。 — 検証方法: Requirement 1.2〜1.4 のテストと Requirement 10.2 の目視 / 状態: 未検証(方針は 2026-09-07 にユーザーが決定。縦 1 面案・奥向き 2 面案は退けた)
3. **格子は 3 × 3(各面 4 本 × 2 方向)、1 px、不透明度 0.16 で、1〜2 px の点を隠さない。** 格子は点より先に描かれ点が上に載るため、点の画素は格子の色に置き換わらない。格子の不透明度が帯の最小 0.45(#3 spec §6.4)より低いため、奥の点が格子と重なっても点のほうが明るい。unit #2 でユーザーが退けた「格子が点の視認を妨げる」への手当てとして、分割数を参考の 3 から増やさない。 — 検証方法: Requirement 2.2〜2.4 と Requirement 10.3 の目視 / 状態: 未検証
4. **目盛りの値は、点群の座標系(各軸 -1〜1)から固定のアフィン写像で導き、Go 側から送らない。** 縦軸 `[-0.10, 2.08]` は参考の値域に合わせたフロントエンドの定数で、`v = lo + (c + 1) / 2 × (hi − lo)` を 3 等分した 4 値を小数第 2 位で書く(`-0.10`・`0.63`・`1.35`・`2.08`)。値は画面の「それらしさ」のためだけにあり、Go 側の生成と関係を持たないため、生成は Go・写像はフロントエンドという分担(request.md §4)を破らない。`ScatterCloud` に値域を足さず、Go 側の契約は変えない。 — 検証方法: Requirement 3.1〜3.2 のテスト / 状態: 未検証(方針は 2026-09-07 にユーザーが承認。Go 側から送る案は退けた)
5. **文字は縦軸の目盛り 4 値と軸名 `Elevation` の計 5 個に留め、床の目盛りは置かない。** roadmap 前提節の「数値 4〜5 個と軸名 1〜2 語」を逐語に読む。床の目盛り(参考では 2 軸 × 4 値)を足すと 8〜12 値になり、手当ての枠を超える。 — 検証方法: Requirement 3.3 / 状態: 未検証(方針は 2026-09-07 にユーザーが承認。床の手前 1 辺に 4 値を足す案・床 2 辺に 8 値を足す案は退けた)
6. **文字はマウント時にオフスクリーン canvas へ描いた画像を、毎フレーム `save` → `transform` → `drawImage` → `restore` で面に沿って貼る。** 文字列は固定(§3 前提 4)なので毎フレーム `fillText` する理由が無く、グリフのラスタライズを 1 度に抑えられる。アフィン変換はラベルの錨(面上の 1 点)における面の 2 方向の投影の微分で作り(3 点を `projectPoint` で投影した差分)、ラベル 1 個の中の透視の歪みは無視する。文字の高さは錨の透視 `scale` に比例して縮む。 — 検証方法: Requirement 3.4〜3.7 と Requirement 10.4 の目視 / 状態: 未検証
7. **影は「間引き 2(点の半分)・1 px・構造 × 8 段の色の表・不透明度 0.22」で描く面すべてに描いても、計測対象 5 パネルの p95 は 20 ms を超えない。** 重みが正の縦面は同時に高々 2 面(§5.2 事後条件)なので、1 フレームに影を描く面は高々 3 面。追加の `fillRect` は 3,000 × 3 面 = 9,000 回、`fillStyle` の切り替えは 24 × 3 面 = 72 回、投影は 3,000 点 × 3 面の乗算(上限の数え上げは縦 4 面ぶんで Requirement 9.5 に置く)。unit #3 の観察(6,000 回の `fillRect` と 576 回の切り替えを足しても p95 は rAF の周期に張り付いたまま)から、同じ桁の追加は収まると見込む。ただし計測器はフレームの間隔を測るため、1 フレームの作業時間の余裕は分からない(`research.md` §2)。 — 検証方法: Requirement 9.1 / 状態: 未検証(方針は 2026-09-07 にユーザーが承認。構造ごとの単色 3 バケットへ最初から落とす案は退け、§8 の削る順の (3) に置いた)。超えた場合の削る順は §8 に定める
8. **背景のノイズは 48 × 48 の値ノイズを枠の大きさへ拡大補間した画像で、参考の「むら」に見える。** 生成はマウント時と枠の寸法の変化時だけで、毎フレームは `drawImage` 1 回。粒が見えないよう、拡大は `imageSmoothingEnabled = true` で行う。 — 検証方法: Requirement 5 のテストと Requirement 10.5 の目視 / 状態: 未検証
9. **既定ピッチを `-0.42` から `+0.42` へ反転し、床を上から見下ろす向きにする。** 現在の値は床を下から見上げる向きである(`research.md` §2 で本セッションが計算で確認。`project.ts` のコメント「斜め上から見た画」と食い違う)。点群だけでは気づきにくいが、床のパネルと格子を描くと向きが露わになり、参考画像(いずれも水平〜上からの視点)と逆になる。ドラッグの向き(#2 spec Requirement 2)は変えない。反転後は `SCATTER_PITCH` の値とコメントの記述が一致するが、同じ食い違いを繰り返さないよう、符号と向きの対応をコメントに明記する(§6.8・Requirement 7.4)。 — 検証方法: Requirement 7 のテストと Requirement 10.1 の目視 / 状態: 未検証(方針は 2026-09-07 にユーザーが承認。現状の向きを保つ案は退けた)
10. **`Vec3` の定義場所を軸線のモジュールからパネルのモジュールへ移しても、`project.ts`・`orbit.ts` の振る舞いは変わらない。** `Vec3` は型だけであり、import 元の変更で実行時の挙動は変わらない。 — 検証方法: `npx tsc --noEmit` と `npm test` / 状態: 未検証
11. **完了時の計測は、`main` にマージ済みの unit #3 の成果(PR #36)を含むビルドで行う。** 着手時の基準は unit #3 完了時の実測(5 パネルとも p95 17.0〜18.0 ms・mean 16.7 ms・max 19〜25 ms。#3 tasks.md Implementation Notes)を用い、再計測しない。 — 検証方法: Requirement 9.6 / 状態: 決定済み(unit #3 の記録を基準とする。中継役の指示 2026-09-07)

## 4. 用語定義

| 用語 | 定義 |
| ---- | ---- |
| パネル(pane) | 単位立方体の面に貼る薄い面。縦 4 面(`x = ±1`・`z = ±1`)と床(`y = -1`)の 5 枚を持ち、毎フレーム床と、重みが正の縦面(高々 2 枚)を描く(§6.1) |
| 奥向き(back-facing) | 面の外向き法線をヨーで回した z 成分が負であること。カメラから見て面の裏側が見えている状態(§5.2) |
| 重み(weight) | 縦面がどれだけ奥を向いているかを表す 0〜1 の連続量。法線の回転後 z 成分の符号反転を 0 で切り上げた値。面の塗り・格子・縁・影・ラベルの不透明度は重みに比例する(§5.2・§6.3) |
| 格子(grid) | パネルの内側を 3 × 3 に分ける線。各面で 2 方向 × 4 本(縁 2 本を含む)(§6.3) |
| 縁(edge) | パネルの外周の線。格子より明るい(§6.3) |
| 目盛り(tick) | 軸の値を示す数値の文字。縦軸(高さ)の 4 値(§6.2) |
| 軸名(axis name) | 軸の名前の文字。`Elevation` の 1 語(§6.2) |
| ラベル(label) | 目盛りと軸名の総称。面上の錨(anchor)と、面に沿う 2 方向(along・up)を持つ(§5.3) |
| 影(shadow) | 点群をパネルの面へ直交投影した点の集合。点の色を保ったまま暗い(§6.4) |
| 地(ground) | 枠の背景の画像。深い青 2 色を低周波のノイズで補間したもの(§6.5) |
| 錨(anchor) | ラベルを置く面上の 1 点(モデル座標)。文字の左端・ベースラインがここに来る |

## 5. 公開インターフェース(API)

ファイルの配置は本書では定めない(dev-decompose の責務)。Go 側の契約(#3 spec §5.1・§6.1〜§6.3)、`subscribeScatter`・`App.Snapshot`(凍結 spec §5.2〜§5.4)、視点の状態機械(#2 spec §5.2・§6.1)、`projectPoint`・`projectPoints`・`createProjectedCloud`(#3 spec §5.3)、パレット(#3 spec §5.4)は変えない。

### 5.1. `Scatter3DPanel`(TypeScript・React コンポーネント・既存の拡張)

- **定義**: `export function Scatter3DPanel(): React.JSX.Element`(シグネチャ・`'use client'`・ポインタ操作・購読は #3 spec §5.2 のまま)
- **事前条件**: #3 spec §5.2 と同じ(`Panel` の本文領域の内側でマウントされる)。
- **事後条件(本 unit で変わる部分)**:
  - 毎フレーム、視点を進めた後、§6.7 の順に描く: 地 → パネルの塗り → 格子と縁 → 影 → 点 → ラベル。3 軸と稜線は描かない。
  - 描くパネルは床と、`wallWeights(orbit.yaw, weights)`(§5.2)が返す重みが正の縦面。縦面ごとに `globalAlpha` を重みにして塗り・格子・縁・影・ラベルを描き、面の描画を終えたら 1 に戻す。床は `globalAlpha = 1`。重みが 0 の縦面は描かない(重み 0 は不透明度 0 と同じ結果なので、飛びにならない)。
  - 地・ラベルの画像・パネルの色文字列・影の色の表は、マウント時(地とラベルの画像は枠の寸法または `devicePixelRatio` の変化時にも)に作り、毎フレームは参照するだけ。
  - 毎フレームの描画で新しいオブジェクト・配列・関数・文字列を作らない(点群の長さが変わったときの器の作り直しと、寸法の変化時の地・ラベルの画像の作り直しを除く)。
  - 点群が空の間もパネル・格子・縁・ラベルを描く(#2 spec Requirement 4.7 の「軸線」をパネルに読み替えて引き継ぐ)。
- **エラー**: #3 spec §5.2 と同じ。トークンの解決に失敗した色は退避色(§6.6)で描き、描画を止めない。オフスクリーン canvas の 2D コンテキストを取得できない場合は、地を `--color-scatter-bg-low` のべた塗り(それも失敗すれば `transparent`)、ラベルを描かないことで退避し、`console.error` に記録する。

### 5.2. パネルの幾何(TypeScript・描画コンポーネントから分離した純粋なモジュール・#2 spec §6.2 の置き換え)

`node --test` で面の重みと影の投影を検証するため、DOM・React・`wailsjs` に依存しない。`Vec3` はこのモジュールが export し、`project.ts` の import 元になる(§3 前提 10)。

- **定義**:
  ```ts
  export type Vec3 = { x: number; y: number; z: number };
  export type WallId = 'xNeg' | 'xPos' | 'zNeg' | 'zPos';
  export type PaneId = WallId | 'floor';
  /** 面の幾何。origin + along × s + up × t(s, t ∈ [0, 2])が面上の点。corners は origin から along・up の順に回る 4 頂点。 */
  export type Pane = {
    id: PaneId;
    origin: Vec3; along: Vec3; up: Vec3;       // along・up は単位ベクトルで直交。up は縦面で (0,1,0)、床で (0,0,1)
    corners: readonly [Vec3, Vec3, Vec3, Vec3];
    /** 影の投影で固定する座標軸と値。xNeg なら axis 'x'・value -1。 */
    fixedAxis: 'x' | 'y' | 'z'; fixedValue: -1 | 1;
  };
  export const PANES: Readonly<Record<PaneId, Pane>>;
  /** 縦 4 面の固定の並び。wallWeights の out の添字と対応する。 */
  export const WALL_IDS: readonly [WallId, WallId, WallId, WallId];  // ['xNeg', 'xPos', 'zNeg', 'zPos']
  export const GRID_DIVISIONS = 3;
  /** 縦 4 面の重み(0〜1)を WALL_IDS の順で out に書く。重み = max(0, −(法線の回転後 z 成分))。 */
  export function wallWeights(yaw: number, out: Float64Array): Float64Array;
  /** 面上の格子線の端点(縁 2 本を含む 2 方向 × (GRID_DIVISIONS + 1) 本)を、pane の s・t の値の並びとして返す。モジュール定数。 */
  export const GRID_STOPS: readonly number[];   // [0, 2/3, 4/3, 2](長さ GRID_DIVISIONS + 1)
  ```
- **入力 / 出力**: `wallWeights` はヨー(ラジアン。正規化されていなくてよい)を受け、長さ 4 以上の `out` に `WALL_IDS` の順で重みを書いて `out` 自身を返す。法線の回転後 z 成分は `xNeg: +sin(yaw)`・`xPos: -sin(yaw)`・`zNeg: -cos(yaw)`・`zPos: +cos(yaw)` で、重みはその符号反転を 0 で切り上げた値(`xNeg: max(0, -sin)`・`xPos: max(0, sin)`・`zNeg: max(0, cos)`・`zPos: max(0, -cos)`)。`PANES` は 5 面の幾何をモジュール定数として持ち、実行時に生成しない。
- **事前条件**: `yaw` は有限値。`out.length ≥ 4`。
- **事後条件**: `wallWeights` は同じ `yaw` につねに同じ値を書く。各重みは 0 以上 1 以下。`xNeg` と `xPos`、`zNeg` と `zPos` は一方が正なら他方は 0(正の重みを持つ縦面は高々 2 面)。重みは `yaw` について連続(離散的な選択を持たない。1° 刻みの隣接差は `sin(1°) < 0.02`)。三角関数は 1 回の呼び出しにつき 2 回だけ評価する。新しいオブジェクト・配列を作らない。`PANES` の各面の `corners` はすべて単位立方体の頂点(座標が ±1 の組)で、`origin + along × 2` と `origin + up × 2` が `corners` に含まれる。
- **エラー**: 返さない・投げない。`yaw` が NaN の場合は 0 とみなす(#2 spec §5.2 の入口の防波堤と同じ規律)。

### 5.3. ラベルの定義と面に沿った変換(TypeScript・純粋なモジュール)

- **定義**:
  ```ts
  export const ELEVATION_RANGE: readonly [number, number];  // [-0.10, 2.08]
  export const TICK_COUNT = 4;
  export const AXIS_NAME = 'Elevation';
  /** range を count − 1 等分した値。両端を含む昇順。 */
  export function tickValues(range: readonly [number, number], count: number): number[];
  /** 小数第 2 位までの文字列(toFixed(2))。負の 0 は "0.00" にする。 */
  export function formatTick(value: number): string;

  export type LabelKind = 'tick' | 'name';
  export type Label = {
    kind: LabelKind; text: string; fontPx: number;
    pane: PaneId;        // 貼り付ける面
    anchor: Vec3;        // 錨(モデル座標。面の外側でよい)
    along: Vec3;         // 文字の進む方向(面に沿う単位ベクトル)
    up: Vec3;            // 文字の高さの方向(面に沿う単位ベクトル。along と直交)
  };
  /** 縦軸のラベル(目盛り 4 個 + 軸名 1 個)を、wall の side 側の縦の辺に沿って作る。マウント時に縦 4 面ぶん(side は 'right')を 1 度 out へ書く。 */
  export function elevationLabels(wall: WallId, side: 'left' | 'right', out: Label[]): Label[];

  export type LabelFrame = { a: number; b: number; c: number; d: number; e: number; f: number; shrink: number };
  /** ラベルの錨における面の 2 方向の投影の微分から、基底の変換(dpr の拡大)に合成するアフィン変換を作る。 */
  export function labelFrame(
    label: Label, yaw: number, pitch: number, view: { width: number; height: number }, dpr: number,
    scratch: [Projected, Projected, Projected], out: LabelFrame,
  ): LabelFrame;
  ```
- **入力 / 出力**: `elevationLabels` は目盛り 4 個(`tickValues(ELEVATION_RANGE, TICK_COUNT)` を `formatTick` した文字列)と軸名 1 個の計 5 個のラベルを `out` に書く(長さ 5)。`side` は `wall` の `along` に対する左右(`left` = `origin` 側の縦の辺、`right` = `origin + along × 2` 側の縦の辺)。錨は `wall` の `side` 側の縦の辺から、面に沿って外側へ `LABEL_GAP = 0.06`(モデル座標)離れた位置(`left` なら `origin − along × LABEL_GAP`、`right` なら `origin + along × (2 + LABEL_GAP)` を通る縦の線上)で、目盛りは値の高さ(`y = -1 + 2 × k / 3`)、軸名は `y` の中央(0)に置く。`labelFrame` は `label.anchor`・`anchor + along × ε`・`anchor + up × ε`(`ε = 0.01`)を `projectPoint` で投影し、その差分を `ε` で割ってモデル座標 1 単位あたりのスクリーン CSS px `(ax, ay)`・`(ux, uy)` を得る(単位ベクトルではない。大きさは `radius = min(view.width, view.height) / 2 × FILL` の桁。`FILL`(0.78)は `project.ts` が export し、本モジュールはそれを import する)。ラベル画像は高さ `fontPx × dpr` px で焼く(§6.2)ため、変換の係数は画像 1 px あたりに換算する。`k = 錨の scale / dpr`(画像の高さ `fontPx × dpr` px を画面の `fontPx × scale` CSS px へ写す係数)とし、`out` には `a = ax / |(ax, ay)| × k`・`b = ay / |(ax, ay)| × k`・`c = -ux / |(ux, uy)| × k`・`d = -uy / |(ux, uy)| × k`(canvas の y 軸は下向きなので up を反転)・`e = anchor の sx`・`f = anchor の sy`(いずれも CSS px)と、`shrink = |(ax, ay)| / (錨の scale × radius)`(面の横方向の縮み。既定の配置で `yaw` を 1° 刻み・`pitch` を可動範囲 ±1.45 rad で走査した実測は最小 0.4955・最大 1.068。透視のため 1 を超えうる。描画側の下限 0.35 は既定の配置では発火しない防波堤)を書く。描画側は基底の変換(`setTransform(dpr, 0, 0, dpr, 0, 0)`)を `save` したうえで `ctx.transform(a, b, c, d, e, f)` を**合成**し(`setTransform` は基底を置換してしまうため使わない)、`drawImage(image, 0, -image.height)` の後に `restore` する。`|(ax, ay)|` または `|(ux, uy)|` が 0 のときは `a`〜`d` を 0 にする(描画側は `shrink` の下限で描かない)。
- **事前条件**: `view.width`・`view.height` が 0 より大きい。`scratch` は effect の寿命で作った器 3 個。
- **事後条件**: `labelFrame` は同じ引数につねに同じ値を書き、新しいオブジェクトを作らない。§6.1 の `along` の向きにより、重みが正の面のラベルでは `a ≥ 0`(文字が画面の左から右へ進む)。`a < 0` のラベル(重み 0 の面に貼った場合にだけ起きる)は描画側が描かない(鏡像を出さない。§6.2)。画像の高さのベクトル `(0, fontPx × dpr)` に `(a, b, c, d)` を掛けた長さは `fontPx × 錨の scale`(CSS px)に等しい(`dpr` によらない)。`tickValues([-0.10, 2.08], 4)` は `-0.10 + k × 2.18 / 3`(`k = 0..3`。誤差 1e-9 以内)、`formatTick` はそれぞれ `"-0.10"`・`"0.63"`・`"1.35"`・`"2.08"` を返す。
- **エラー**: 返さない・投げない。`yaw`・`pitch` が NaN の場合は 0 とみなす。

### 5.4. 影の投影(TypeScript・`project.ts` への追加)

- **定義**:
  ```ts
  /** points を、axis の座標を value に固定した面へ直交投影してから、projectPoints と同じ変換で out に書く。stride 個おきに 1 点を採る。 */
  export function projectPointsOnto(
    points: readonly Vec3[], axis: 'x' | 'y' | 'z', value: -1 | 1, stride: number,
    yaw: number, pitch: number, view: { width: number; height: number }, out: ProjectedCloud,
  ): void;
  ```
- **入力 / 出力**: `points[0], points[stride], points[2 × stride], …` の各点について、`axis` の成分を `value` に置き換えた点を #3 spec §5.3 と同じ式で投影し、`out.sx[j]`・`out.sy[j]`・`out.depth[j]`(`j` は採った順)に書き、`out.length` を採った点数(`ceil(points.length / stride)`)にする。
- **事前条件**: `stride` は 1 以上の整数。`out` の容量が `ceil(points.length / stride)` 以上。
- **事後条件**: 同じ引数につねに同じ値を書く。`out.sx[j]`・`out.sy[j]`・`out.depth[j]` は、`axis` の成分を `value` に置き換えた点を `projectPoint` に渡した結果と 1e-3 以内で一致する。三角関数は 1 回の呼び出しにつき 4 回だけ評価する。新しいオブジェクト・配列を作らない。
- **エラー**: 返さない・投げない。容量不足は `projectPoints` と同じく容量までに切り詰めて `console.error` に記録する。`stride` が 1 未満または非整数の場合は 1 として扱う。

### 5.5. 地のノイズ(TypeScript・純粋なモジュール)

- **定義**:
  ```ts
  export const NOISE_SIZE = 48;
  /** size × size の値ノイズ(0〜1)を決定的に作る。seed が同じなら同じ配列。 */
  export function buildNoise(size: number, seed: number): Float32Array;
  ```
- **入力 / 出力**: 長さ `size × size` の `Float32Array`。行優先。値は 0 以上 1 以下。
- **事前条件**: `size` は 2 以上の整数。`seed` は有限値。
- **事後条件**: 同じ `seed` につねに同じ配列を返す(疑似乱数は `Math.random` を使わず、`seed` から決定的に作る)。隣り合う要素(上下左右)の差の絶対値は 0.25 以下(低周波。粒に見えない)。値の最小と最大の差は 0.3 以上(むらが見える)。上下左右の端で隣接を循環させる(端の継ぎ目が出ない)。
- **エラー**: `size` が 2 未満または非整数の場合は例外を投げる(プログラマの誤り)。

### 5.6. テストの実行コマンド

#2 spec §5.4 のまま(`cd frontend && npm test` → `node --test "src/lib/**/*.test.ts"`)。本 unit は §5.2〜§5.5 のモジュールのテストを足し、`axes.test.ts`(#2 spec Requirement 4.1〜4.2)を §5.2 のテストで置き換える。Go のテストは `go test ./...`(本 unit で変えない)。

## 6. データ構造

### 6.1. パネルの幾何(`PANES`。#2 spec §6.2 `AXIS_SEGMENTS` の置き換え)

| id | origin | along | up | fixedAxis / fixedValue |
| :-- | :-- | :-- | :-- | :-- |
| `xNeg` | (-1, -1, 1) | (0, 0, -1) | (0, 1, 0) | x / -1 |
| `xPos` | (1, -1, -1) | (0, 0, 1) | (0, 1, 0) | x / 1 |
| `zNeg` | (-1, -1, -1) | (1, 0, 0) | (0, 1, 0) | z / -1 |
| `zPos` | (1, -1, 1) | (-1, 0, 0) | (0, 1, 0) | z / 1 |
| `floor` | (-1, -1, -1) | (1, 0, 0) | (0, 0, 1) | y / -1 |

- **不変条件**: `along`・`up` は単位ベクトルで直交し、`origin + along × 2`・`origin + up × 2`・`origin + along × 2 + up × 2` が `corners` の残り 3 頂点。すべての頂点は単位立方体の頂点。縦 4 面の `along` は、面の内側(原点側)から見て左から右へ進む向きに取る(`along = n × up`。`n` は外向き法線)。`projectPoint` の式(`x1 = x cos yaw + z sin yaw`、画面右が `x1` の正)では `along` の回転後 x 成分が「その面の回転後の法線 z 成分の符号反転」に等しく、重みが正の面(法線 z 成分 < 0)ではつねに 0 より大きい。したがって描く面に貼った文字は、どの視点でも画面の左から右へ進み、鏡像にならない(§5.3 の `a < 0` は重みが正の面では起きず、防波堤としてだけ残す)。
- **ロジックの所在**: 重みの計算・格子線の端点・影の投影先の座標は §5.2・§5.4 のモジュールに集約する。`Scatter3DPanel` は識別子で面を引き、投影して描くだけ。
- **置き換え**: #2 spec §6.2 の `AXIS_SEGMENTS`(`axis` 3 本・`edge` 12 本)と `Segment` 型を廃する。

### 6.2. ラベル(§5.3)

- **縦軸の目盛り**: `ELEVATION_RANGE = [-0.10, 2.08]` を 3 等分した 4 値(両端を含む)。`k = 0..3` の目盛りの錨は `y = -1 + 2k/3`。文字は `formatTick` の結果(`-0.10`・`0.63`・`1.35`・`2.08`)。`fontPx = 13`、太字。
- **軸名**: `Elevation`。錨は `y = 0`(目盛り `0.63` と `1.35` の間)。`fontPx = 14`、太字。
- **貼る面と辺**: 縦 4 面それぞれの `right` 側の縦の辺(`origin + along × 2` 側。§6.1 の向きにより、重みが正の面ではその面の画面上の右の辺)に貼る。ラベルは 4 面 × 5 個 = 20 個をマウント時に 1 度 `elevationLabels` で作り、毎フレームは面の重みを `globalAlpha` にして描く(重み 0 の面のラベルは描かない)。参考の「右側の 1 列」と同じ配置に、最も奥の面の列が最も濃く見える。
- **可読性の下限**: `labelFrame` の `a` が負のラベルは描かない(鏡像を出さない。§6.1 の向きにより重みが正の面では起きない)。`shrink` が 0.35 未満のラベルは描かない(真横に近い面の文字は潰れて読めず、汚れに見えるため)。`fontPx × scale` が 8 px 未満になるラベルも描かない。
- **画像**: 各ラベルの文字はマウント時に `OffscreenCanvas`(または `document.createElement('canvas')`)へ `devicePixelRatio` 倍の解像度で 1 度描く。書体は `bold {fontPx}px ui-sans-serif, system-ui, sans-serif`(参考の太字サンセリフ。等幅の `--font-mono` を使わないのは、数字の字幅が揃いすぎて参考の見え方から離れるため)。色は `--color-scatter-label`(§6.6)、不透明度 0.9。
- **不変条件**: ラベルは 1 面あたり 5 個(計 20 個)で固定(§3 前提 5)。文字列は英語(CLAUDE.md 言語規約)。

### 6.3. パネル・格子・縁の描画規則

- **重みと `globalAlpha`**: 縦面は `wallWeights` の重みが正のものだけを描き、面ごとに `globalAlpha = 重み` を設定して塗り・格子・縁・影・ラベルを描き、面の描画を終えたら `globalAlpha = 1` に戻す。床は `globalAlpha = 1` で描く。色文字列の不透明度(下記)は「最も奥を向いたとき(重み 1)」の値になる。点の描画では `globalAlpha` を触らない(#3 spec §6.4 のまま)。
- **塗り**: 各面の 4 頂点を `projectPoint` で投影し、1 つのパスを `fill` する。色は `--color-scatter-pane` に不透明度 0.06 を焼き込んだ `rgba()` 文字列(マウント時に作る)。
- **格子**: 各面で `GRID_STOPS` の内側の値(2/3・4/3)について、`along` 方向と `up` 方向の線を引く(内側 2 本 × 2 方向 = 4 本)。面ごとに 4 本を 1 つのパスにまとめて `stroke` 1 回。線幅 1 CSS px、色は `--color-scatter-pane` に不透明度 0.16。
- **縁**: 各面の外周 4 辺(共有する辺は重ねて描いてよい)を 1 つのパスにまとめて `stroke` 1 回。線幅 1 CSS px、色は `--color-scatter-pane` に不透明度 0.45。
- **奥行きによる不透明度の変化は行わない**(#2 spec §3 前提 1 の置き換え)。線の不透明度は面の重みだけで決まる。
- **投影の器**: 面の頂点・格子線の端点の投影は `projectPoint` に effect の寿命で作った器を渡す。器の数は「頂点 4 個 + ラベルの微分用 3 個」で足り、格子線の端点は頂点の投影から線形補間で求める(面上の点の投影は透視で線形にならないが、縁の 2 点から 1/3・2/3 を取る誤差は格子線の太さ 1 px に対し 1〜3 px で、参考との見え方の差に現れない。端点を毎回投影する案は 2026-09-07 にユーザーが退けた)。投影は面ごとに頂点の 4 回に限る。

### 6.4. 影の描画規則

- **対象の面**: 描く面すべて(床と、重みが正の縦面)。縦面の影は面の `globalAlpha`(重み)の下で描く。
- **間引き**: `SHADOW_STRIDE = 2`(点の半分)。
- **色**: 構造 × `SHADOW_STEPS = 8` 段の表。段 `s` の色は、構造のパレットと同じ停止色を 8 段に補間した RGB に `SHADOW_ALPHA = 0.22` を焼き込んだ `rgba()` 文字列。地形は位相ごとに表を持つ(36 位相 × 8 段)。表は `buildRamp` と同じモジュールに置く `buildShadowRamp(stops, steps, alpha): string[]` で作り、`rgba(` の文字列テンプレートがそのモジュールの外へ出ない規律を保つ。
- **バケット**: `shadowKey = S × SHADOW_STEPS + min(SHADOW_STEPS − 1, floor(C × SHADOW_STEPS))`(0〜23)。間引いた点について counting sort でバケット順に並べた添字の配列を 1 フレームに 1 度作り(器は点群の長さが変わったときだけ作り直す)、描く面すべてで使い回す。面ごとに `projectPointsOnto` で投影し、バケットの昇順に `fillStyle` を 1 回ずつ設定して 1 CSS px の `fillRect` で描く。
- **不透明度の帯は持たない**(影は奥行きで変えない)。
- **点との関係**: 影は点より先に描く。点が影の上に載る。

### 6.5. 地(背景)の生成規則

- `buildNoise(NOISE_SIZE, NOISE_SEED)`(`NOISE_SEED = 20260907` の定数)の各値 `n` で、`--color-scatter-bg-low` と `--color-scatter-bg-high` を線形補間した色を、48 × 48 の `ImageData` に書き、オフスクリーン canvas へ `putImageData` する。
- 枠の寸法(CSS px × `devicePixelRatio`)のオフスクリーン canvas へ、`imageSmoothingEnabled = true` で拡大して `drawImage` する。この 2 段目の canvas を毎フレーム `drawImage` で枠へ写す(1 回)。
- 生成はマウント時と、枠の寸法または `devicePixelRatio` が変わったときだけ。
- `--color-surface-1` は散布図の背景として使わなくなる(トークンは他パネルが使うため残す)。

### 6.6. デザイントークン(CSS・`globals.css` の `@theme` への追加)

```css
/* 3D 散布図の地(背景)。深い青 2 色をノイズで補間する。 */
--color-scatter-bg-low:  #061024;
--color-scatter-bg-high: #0e2244;
/* パネルの塗り・格子・縁の基本色。不透明度は描画側で焼き込む(0.06 / 0.16 / 0.45)。 */
--color-scatter-pane:    #9fbcf0;
/* 目盛りと軸名の文字色。 */
--color-scatter-label:   #e8eef8;
```

- 色の直値は `globals.css` にだけ置く(凍結 spec Requirement 9.1 を維持)。不透明度の値(0.06・0.16・0.45・0.22・0.9)は色ではなく数値の定数として `.ts` に置く(#3 spec `BAND_ALPHA` と同じ扱い)。
- **退避**: いずれかのトークンが空文字または `parseHex` が `null` を返す文字列だった場合、地は `'transparent'`、パネル・格子・縁は `'gray'`、文字は `'white'` で描き、`console.error` に記録する(凍結 spec Requirement 9.2 と同じ退避色)。影の色は #3 spec §6.5 の停止色から作るため、その退避(`'white'`)に従う。

### 6.7. 描画順(1 フレーム)

1. 地(`drawImage` 1 回)
2. 床、次に重みが正の縦面(`WALL_IDS` の順)について、面ごとに `globalAlpha` を重み(床は 1)にして: 塗り(`fill` 1 回)→ 格子(`stroke` 1 回)→ 縁(`stroke` 1 回)→ 影(24 バケット以下の `fillStyle`、`fillRect` は ceil(点数 / 2) 回以下)。面を終えたら `globalAlpha = 1`
3. 点(#3 spec §6.4 のまま。576 バケット以下。`globalAlpha = 1`)
4. ラベル(重みが正の縦面について、面ごとに `globalAlpha` を重みにして 5 個以下の `save` + `transform` + `drawImage` + `restore`。終えたら `globalAlpha = 1`)

影を面ごとに(点より先に)描くのは、面の `globalAlpha` を 1 度だけ設定するため。ラベルを点より後に描くのは、文字を点で隠さないため。

### 6.8. 既定ピッチ(`SCATTER_PITCH`)

`SCATTER_PITCH = +0.42`(#3 spec §5.3「既存値 -0.42 のまま」の置き換え。§3 前提 9)。`orbit.ts` はこの定数を import して初期値と復帰先に使うため、変更は `project.ts` の 1 箇所で済む。`PITCH_LIMIT`・`DRAG_RAD_PER_PX`・ドラッグの向きは変えない。

`project.ts` の `SCATTER_PITCH` のコメント「点群を斜め上から見た画にする」は、現状の値 `-0.42` では実際の画(床を下から見上げる)と食い違っている(§3 前提 9)。反転後は記述と値が一致するが、符号だけでは向きが読めないため、コメントに「正の値で床を上から見下ろす(奥の辺が画面で上に来る)」という符号と向きの対応を書き添える。値を変えるだけでコメントを直さない、という再発を防ぐための規定。

## 7. 振る舞い(受け入れ基準)

### Requirement 1: パネルの幾何と縦面の重み

**対象**: §5.2 パネルの幾何 / §6.1 `PANES`

**受け入れ基準**:
1.1. システムは、`PANES` に縦 4 面と床の 5 面を持ち、各面の `corners` がすべて単位立方体の頂点で、`origin + along × 2`・`origin + up × 2`・`origin + along × 2 + up × 2` を含まなければならない。(常時)
1.2. `wallWeights(yaw, out)` が呼ばれたとき、システムは `WALL_IDS` の順に `max(0, -sin yaw)`・`max(0, sin yaw)`・`max(0, cos yaw)`・`max(0, -cos yaw)` を `out` に書き(各 1e-9 以内)、`out` と同一の参照を返さなければならない。(イベント)
1.3. システムは、`yaw` を 0 から 2π まで 1° 刻みで進めたとき、各面の重みの隣接する `yaw` どうしの差の絶対値を 0.02 以下に保たなければならない(連続。離散的な入れ替わりが無い)。(常時)
1.4. システムは、任意の `yaw` で `xNeg`・`xPos` の重みの積と `zNeg`・`zPos` の重みの積を 0 にし、正の重みを持つ縦面を高々 2 面にしなければならない。(常時)
1.5. `yaw` が NaN の場合、システムは 0 として扱い、例外を投げてはならない。(異常系)
1.6. システムは、`wallWeights` と `PANES` の参照において新しいオブジェクト・配列を作ってはならず、`wallWeights` の中で `Math.sin`・`Math.cos` を合わせて 2 回だけ評価しなければならない。(常時)
1.7. システムは、`GRID_DIVISIONS` を 3、`GRID_STOPS` を `[0, 2/3, 4/3, 2]`(各 1e-9 以内)、`WALL_IDS` を `['xNeg', 'xPos', 'zNeg', 'zPos']` にしなければならない。(常時)

### Requirement 2: パネル・格子・縁の描画

**対象**: §5.1 `Scatter3DPanel` / §6.3 パネル・格子・縁の描画規則

(2.1〜2.5・2.7〜2.8 は `Scatter3DPanel.tsx` の静的検査、2.6 は点群の購読を止めた状態での目視で確かめる)

**受け入れ基準**:
2.1. 描画のとき、システムは床と、`wallWeights` の重みが正の縦面だけを描き、重みが 0 の縦面と上面を描いてはならない(縦面のループが重みの正の比較で囲われている)。(イベント)
2.2. 描画のとき、システムは各面の塗りを `--color-scatter-pane` の不透明度 0.06、格子を不透明度 0.16、縁を不透明度 0.45 の色文字列で描き、縦面ごとに `globalAlpha` を重みに設定し、面の描画を終えたら 1 に戻さなければならない(`globalAlpha` の代入が面のループの入口と出口の 2 箇所に限られ、点の描画の中に無い)。(イベント)
2.3. 描画のとき、システムは各面の格子を内側 2 本 × 2 方向とし、面ごとに格子線を 1 回の `stroke`、縁を 1 回の `stroke` で描かなければならない(描画関数の中の `stroke(` が面のループの中の 2 箇所)。(イベント)
2.4. システムは、格子線と縁の線幅を 1 CSS ピクセルに保たなければならない。(常時)
2.5. 描画のとき、システムは面の頂点の投影に effect の寿命で作った器を渡し、描画関数の中で `Projected` を作ってはならない。(イベント)
2.6. 点群が空の間、システムはパネル・格子・縁・ラベルを描き続けなければならない。(状態)
2.7. 描画のとき、システムは格子線の端点を面の頂点 4 個の投影から線形補間で求め、格子線の端点のために `projectPoint` を呼んではならない(投影は面ごとに頂点の 4 回)。(イベント)
2.8. 描画のとき、システムは面の重みを `wallWeights` から 1 フレームに 1 度だけ求め、面の描画とラベルの描画で同じ器を読まなければならない(`wallWeights(` の呼び出しが描画関数の中に 1 箇所)。(イベント)

### Requirement 3: 目盛りと軸名

**対象**: §5.3 ラベルの定義と面に沿った変換 / §6.2 ラベル

(3.1〜3.7・3.11〜3.13 はテスト(3.7 の下限は既定の配置では発火しないため、重み 0 の面など人為的な入力で確かめる)、3.8 は `Scatter3DPanel.tsx` の静的検査(描画関数の中に `fillText` が無く、ラベルの `drawImage` が 1 箇所)、3.9 は静的検査(描画順と、ラベルの `transform(` が `save(`・`restore(` で挟まれ、描画関数の中に `setTransform(` が基底の 1 箇所しか無い)、3.10 は静的検査(`elevationLabels` の呼び出しがマウント時の経路にだけあり、描画関数の中に無い)で確かめる。見た目は Requirement 10.4 の目視)

**受け入れ基準**:
3.1. `tickValues([-0.10, 2.08], 4)` が呼ばれたとき、システムは長さ 4 の昇順の配列を返し、`k` 番目の値を `-0.10 + k × 2.18 / 3`(`k = 0..3`)と 1e-9 以内で一致させなければならない。(イベント)
3.2. `formatTick` が呼ばれたとき、システムは小数第 2 位までの文字列を返し、`-0.10`・`0.63`・`1.35`・`2.08` の 4 値と、`-0.001` に対して `"0.00"`(負の 0 を出さない)を返さなければならない。(イベント)
3.3. システムは、画面に描く文字を面ごとに目盛り 4 個と軸名 `Elevation` 1 個の計 5 個に限らなければならない(`elevationLabels` の出力が長さ 5。縦 4 面で 20 個)。(常時)
3.4. `labelFrame` が呼ばれたとき、システムは `a`・`b` を錨における `along` 方向の投影の微分(`anchor + along × 0.01` と `anchor` の `projectPoint` の差 ÷ 0.01)の単位ベクトル × `k`(`k = 錨の scale / dpr`)、`c`・`d` を `up` 方向の投影の微分の単位ベクトルの符号反転 × `k` にしなければならない(`yaw = 0`・`pitch = SCATTER_PITCH`・`dpr = 1` の `zNeg` 面で、テストが `projectPoint` から独立に計算した値と 1e-6 以内で一致)。(イベント)
3.5. システムは、`yaw` を 0 から 2π まで 1° 刻みで進め、各 `yaw` で `wallWeights` の重みが正の面に貼った `elevationLabels` の全ラベルについて `labelFrame` の `a` を求めたとき、すべて 0 以上でなければならない(重みが正の面の文字は左から右へ進む)。`a` が負のラベルがあれば、システムはそれを描いてはならない(鏡像を出さない)。(常時)
3.6. システムは、`yaw` を変えたとき `labelFrame` の `a`〜`f` の少なくとも 1 つが変わること(ラベルが画面に固定されていないこと)を、`yaw = 0` と `yaw = 0.5` の比較で示さなければならない。(常時)
3.7. `shrink` が 0.35 未満、または `fontPx × 錨の scale` が 8 未満のラベルについて、システムはそのラベルを描いてはならない。(異常系)
3.8. 描画のとき、システムはラベルの画像をマウント時(または寸法・`devicePixelRatio` の変化時)に作ったオフスクリーン canvas から `drawImage` し、毎フレーム `fillText` を呼んではならない。(イベント)
3.9. 描画のとき、システムはラベルを点より後に描き、ラベルごとの `transform` を `save` / `restore` で挟んで基底の変換(`devicePixelRatio` の拡大)に合成し、`setTransform` で基底を置換してはならない。(イベント)
3.10. システムは、`elevationLabels` をマウント時に縦 4 面ぶん呼ぶだけで、毎フレームの描画の中で呼んではならない。(状態)
3.11. `labelFrame` が呼ばれたとき、システムは `e`・`f` を錨の `projectPoint` の `sx`・`sy`(CSS px)と 1e-9 以内で一致させなければならない。(イベント)
3.12. `labelFrame` が呼ばれたとき、システムは `shrink` を `|along 方向の微分| / (錨の scale × radius)`(`radius = min(view.width, view.height) / 2 × FILL`)と 1e-6 以内で一致させなければならない。(イベント)
3.13. `labelFrame` が呼ばれたとき、システムは画像の高さのベクトル `(0, fontPx × dpr)` に `(a, b, c, d)` を掛けた長さを `fontPx × 錨の scale` と 1e-6 以内で一致させなければならず、これを `dpr = 1` と `dpr = 2` の両方で満たさなければならない(画像 px から画面 CSS px への写像)。(イベント)
3.14. `labelFrame` は、同じ引数につねに同じ値を書き、新しいオブジェクトを作ってはならない。(常時)

### Requirement 4: 影

**対象**: §5.4 影の投影 / §6.4 影の描画規則

(4.1〜4.4・4.6 はテスト、4.5 は `Scatter3DPanel.tsx` の静的検査(影の `fillStyle` の代入がバケットのループの中に 1 箇所、`fillRect` の幅と高さが 1)、4.7 は静的検査(描画順)、4.8 は静的検査(バケット順の配列を作る呼び出しが面のループの外に 1 箇所で、`.sort(` が 0 件)で確かめる。見た目は Requirement 10.6 の目視)

**受け入れ基準**:
4.1. `projectPointsOnto(points, axis, value, stride, …)` が呼ばれたとき、システムは `ceil(points.length / stride)` 個の点を書き、各点の `sx`・`sy`・`depth` を「`axis` の成分を `value` に置き換えた点を `projectPoint` に渡した結果」と 1e-3 以内で一致させなければならない。(イベント)
4.2. `stride` が 1 未満または非整数の場合、システムは 1 として扱い、例外を投げてはならない。(異常系)
4.3. `out` の容量が不足する場合、システムは容量までの点を書いて `console.error` に記録し、例外を投げてはならない。(異常系)
4.4. システムは、`projectPointsOnto` の中で `Math.cos`・`Math.sin` を合わせて 4 回だけ評価しなければならない。(常時)
4.5. 描画のとき、システムは影を描く面すべて(床と重みが正の縦面)に、間引き 2・1 CSS ピクセルの `fillRect`・構造 × 8 段の表の色で描き、面ごとの `fillStyle` の設定を 24 回以下にしなければならない。(イベント)
4.6. `buildShadowRamp(stops, 8, 0.22)` が呼ばれたとき、システムは 8 本の `rgba(r, g, b, 0.22)` 文字列を返し、段 0 の RGB を `stops[0]`、段 7 の RGB を最後の停止色に一致させなければならない。(イベント)
4.7. 描画のとき、システムは影を点より先、同じ面の塗り・格子・縁より後に描かなければならない。(イベント)
4.8. 描画のとき、システムは影のバケット順の配列を 1 フレームに 1 度だけ作り、描く面すべてで使い回さなければならない(counting sort。比較ソートを使わない)。(イベント)

### Requirement 5: 地(背景)

**対象**: §5.5 地のノイズ / §6.5 地の生成規則

(5.1〜5.4 はテスト、5.5 は `Scatter3DPanel.tsx` の静的検査(地の描画が `drawImage` 1 箇所で、描画関数の中に枠全体を塗る `fillRect` が無い)、5.6 は静的検査(画像を作る呼び出しが寸法と `devicePixelRatio` の比較で囲われ、毎フレームの経路に無い)、5.7 は静的検査(`getContext` の `null` 分岐)で確かめる。見た目は Requirement 10.5 の目視)

**受け入れ基準**:
5.1. `buildNoise(48, seed)` が呼ばれたとき、システムは長さ 2304 の `Float32Array` を返し、すべての値を 0 以上 1 以下にしなければならない。(イベント)
5.2. システムは、同じ `seed` に対してつねに同じ配列を返さなければならない(`Math.random` を使わない)。(常時)
5.3. システムは、上下左右に隣り合う要素の差の絶対値を 0.25 以下に保ち(端は循環)、全体の最大と最小の差を 0.3 以上にしなければならない。(常時)
5.4. `size` が 2 未満または非整数の場合、システムは例外を投げなければならない。(異常系)
5.5. 描画のとき、システムは地を `drawImage` 1 回で描き、`fillRect` によるべた塗りを背景に使ってはならない。(イベント)
5.6. 枠の寸法または `devicePixelRatio` が変わったとき、システムは地とラベルの画像を作り直さなければならず、それ以外のフレームで作り直してはならない。(イベント)
5.7. オフスクリーン canvas の 2D コンテキストを取得できない場合、システムは地を `--color-scatter-bg-low` のべた塗りで退避し、`console.error` に記録し、描画を止めてはならない。(異常系)

### Requirement 6: 3 軸と稜線の置き換え

**対象**: §5.2 パネルの幾何 / §6.1 `PANES`

**受け入れ基準**:
6.1. システムは、`AXIS_SEGMENTS`・`Segment` を持ってはならず、原点を通る 3 軸を描いてはならない(`grep -n "AXIS_SEGMENTS\|kind: 'axis'" frontend/src` が 0 件)。(常時)
6.2. システムは、`Vec3` を §5.2 のモジュールから export し、`project.ts`・`orbit.ts` の振る舞いを変えてはならない(`orbit.test.ts`・`project.test.ts` が変更なしで成功する。ただし `SCATTER_PITCH` の値に依存する検査は §6.8 の値で読む)。(常時)
6.3. システムは、`axes.test.ts`(#2 spec Requirement 4.1〜4.2)を §5.2 のテストで置き換えなければならない。(常時)
6.4. 描画のとき、システムは奥行きによる線の不透明度の変化(#2 spec Requirement 4.3〜4.4)を行ってはならない(線の不透明度を変えるのは面の重みだけ)。(イベント)

### Requirement 7: 既定ピッチ

**対象**: §6.8 既定ピッチ

**受け入れ基準**:
7.1. システムは、`SCATTER_PITCH` を `+0.42` にし、`createOrbit()` の `pitch`・`returnFrom` をその値にしなければならない。(常時)
7.2. `yaw = 0`・`pitch = SCATTER_PITCH` で床の手前の辺(`z = 1`)と奥の辺(`z = -1`)を投影したとき、システムは奥の辺の `sy` を手前の辺の `sy` より小さく(画面で上に)しなければならない(上から見下ろす向き)。(イベント)
7.3. システムは、ドラッグの向き(#2 spec Requirement 2.1〜2.2)と `PITCH_LIMIT`・`RETURN_MS` を変えてはならない。(常時)
7.4. システムは、`SCATTER_PITCH` のコメントに、正の値で床を上から見下ろす向き(奥の辺が画面で上に来る)になることを明記していなければならない(`grep -n "見下ろす" frontend/src/lib/project.ts` が 1 件以上)。(常時)

### Requirement 8: 既存の振る舞いの維持

**対象**: §5.1 `Scatter3DPanel`

**受け入れ基準**:
8.1. システムは、#3 spec Requirement 1〜7(点群の生成・`ScatterPoint`・構造の幾何・購読・投影の器渡し・カラーマップ・点の描画)の振る舞いを変えてはならず、Go 側のファイルと `scatterpoint.go`・`scattersource.go`・`feed.ts`・`framestats.ts` を変更してはならない。(常時)
8.2. システムは、#2 spec Requirement 1〜3・6〜8(ドラッグ・操作量・復帰・ポインタの離脱・カーソル・既存の維持)の振る舞いを変えてはならず、`orbit.ts` を変更してはならない(`SCATTER_PITCH` は `project.ts` で変わる)。(常時)
8.3. システムは、凍結 spec Requirement 7(回転の継続)・8(描画領域への追随)・9(デザイントークンへの準拠)の振る舞いを変えてはならない。(常時)
8.4. `view.width` または `view.height` が 0 の場合、システムは何も描かず、次のフレームへ進まなければならない。(異常系)
8.5. システムは、§6.6 の 4 個のトークンを `globals.css` の `@theme` に持ち、地・パネル・格子・縁・文字の色をそれらから解決しなければならず、`.ts`・`.tsx` に色の直値を書いてはならない(退避色 `'white'`・`'transparent'`・`'gray'` を除く。`rgba(` の文字列テンプレートはパレットのモジュールの中に限る)。(常時)

### Requirement 9: 検証手段の成立と描画の負荷(非機能)

**対象**: §5.1 `Scatter3DPanel` / §5.6 テストの実行コマンド / §6.7 描画順

(9.1・9.6 は完了時の計測、9.2 はコマンドの実行、9.3 は `Scatter3DPanel.tsx` の静的検査(描画関数の中に `new`・配列リテラル・オブジェクトリテラル・アロー関数・テンプレート文字列・`concat`・`map`・`filter` が無く、除外の 2 箇所が比較で囲われている)、9.4 は静的検査(`grep -n \"arc(\|\.sort(\" frontend/src/components/Scatter3DPanel.tsx frontend/src/lib/scatter*.ts` が 0 件)、9.5 は静的検査(呼び出し回数の数え上げ)で確かめる)

**受け入れ基準**:
9.1. 完了時に `wails build -devtools` のビルドを起動し `window.nullops.enableFrameStats()` で計測したとき(手順は凍結済み `docs/specs/001-dashboard-mvp/005-framestats-runtime/tasks.md` B 節。1440×900)、システムは計測対象 5 パネル(`commit`・`depgraph`・`gauge`・`scatter`・`timeseries`)のいずれについても、報告の最後の 6 行(30 秒ぶん)すべてで p95 が 20 ミリ秒以上である状態にしてはならない(#3 spec Requirement 9.1 と同じ集計規則)。(イベント)
9.2. システムは、`go vet ./...`・`go test ./...`・`cd frontend && npm run lint`・`cd frontend && npm test`・`wails build` がいずれもエラーなく終了する状態を保たなければならない。(常時)
9.3. システムは、毎フレームの描画の中で新しいオブジェクト・配列・関数・文字列を作ってはならない(点群の長さが変わったときの器の作り直し、寸法・`devicePixelRatio` の変化時の画像の作り直しを除く)。(常時)
9.4. システムは、地・パネル・格子・縁・影・ラベルの描画に `arc` と比較ソートを使ってはならない。(常時)
9.5. システムは、縦 4 面 + 床の 5 面を数えた上限として、1 フレームの `fillStyle` の設定回数を 576(点)+ 120(影 24 × 5 面)+ 5(塗り 5 面)= 701 回以下、`strokeStyle` の設定回数を 10 回以下(格子・縁 × 5 面)、`fillRect` の呼び出し回数を 点数 + 5 × ceil(点数 / 2) 回以下、`stroke` を 10 回以下、`fill` を 5 回以下、`drawImage` を 1(地)+ 20(ラベル 5 個 × 縦 4 面)= 21 回以下、`globalAlpha` の代入を 10 回以下(縦面ごとに入口と出口。ラベルの面ループを含めれば 16 回以下)に保たなければならない(#3 spec Requirement 9.6 の置き換え。重みが正の縦面は高々 2 面なので、実際の回数は縦 2 面 + 床ぶんに収まる)。(常時)
9.6. システムは、完了時の計測の p95・mean・max(5 パネルぶん)を unit の `tasks.md` Implementation Notes に記録していなければならない(着手時の基準は §3 前提 11 のとおり unit #3 完了時の値を転記する)。(常時)

### Requirement 10: 配布ビルドの目視(人間による承認)

**対象**: §5.1 `Scatter3DPanel`

**受け入れ基準**:
10.1. 配布ビルドを起動して放置したとき、システムは床を上から見下ろす向きで、奥を向く縦面(濃さは奥向きの度合いに応じる)と床に薄い面・3 × 3 の格子・縁を描き、それらが点群と同じ回転で動かなければならない(画面に固定されない)。(イベント)
10.2. 視点をドラッグで 1 周回したとき、システムは面とラベルが突然消える・現れる事象を起こしてはならない(面とラベルは奥向きの度合いに応じて連続に濃くなり・薄くなる)。(イベント)
10.3. 格子と点を見比べたとき、システムは格子線が 1〜2 px の点を隠して構造が読めなくなる事象を起こしてはならない。(イベント)
10.4. 目盛りと軸名を見たとき、システムは 4 値と `Elevation` が面に沿って歪み、視点を回すと一緒に回り、どの視点でも鏡像にならず、読めないほど潰れた文字を残していてはならない。(イベント)
10.5. 背景を見たとき、システムは一様な黒ではなく、むらのある深い青の地を描いていなければならない(粒が見えない)。(イベント)
10.6. 影を見たとき、システムは点群の投影が奥を向く縦面と床に点の色を保ったまま暗く写り、視点を回すと面と一緒に回っていなければならない。(イベント)
10.7. システムは、unit #3 の点群・色・構造(#3 spec Requirement 10.1〜10.6)と unit #2 の操作(#2 spec Requirement 10)を引き続き満たさなければならない。(常時)

## 8. 実現方針

- **縦 4 面 + 床、重みによる連続な不透明度**: §3 前提 2。参考は縦 1 面だが、自動回転の下で飛びと鏡像を避けるための読み替え(2026-09-07 にユーザーが決定)。面ごとの不透明度は `globalAlpha` で与え、色文字列は重み 1 のときの値で 1 度だけ作る(重みごとに文字列を作らない)。
- **格子線の端点**: §6.3。頂点 4 個の投影から線形補間する。器 7 個・投影 4 回/面で済み、誤差 1〜3 px は 1 px の線では見分けがつかない。
- **描画順と器**: 地 → 面ごとに(塗り → 格子・縁 → 影)→ 点 → ラベル(§6.7)。器は effect の寿命で 7 個(頂点 4 + ラベルの微分 3)と重みの `Float64Array(4)` 1 個、影の `ProjectedCloud` は 1 個を描く面すべてで使い回す(面ごとに投影 → 描画を順に行う)。
- **文字の画像化**: §3 前提 6。`fillText` を毎フレーム呼ばず、`drawImage` にアフィン変換を掛ける(基底の `dpr` 拡大に `transform` で合成する)。透視の歪みは錨での微分で近似する。`radius` の計算に要る `FILL` は `project.ts` から export する(値の二重管理を避ける)。
- **影の色の表**: 点のパレット(64 段 × 3 帯)を影に流用すると `fillStyle` の切り替えが 3 面で 1,728 回になるため、8 段・帯なしの表(24 バケット)を別に持つ。間引き 2 は `fillRect` の増分を 9,000 回に抑えるため。
- **p95 が 20 ms を超えた、または max が 50 ms を超えたときの削る順**: (1) `SHADOW_STRIDE` を 2 → 4(影の `fillRect` を 4,500 回へ)、(2) 影を床だけに(縦面の影を消す)、(3) `SHADOW_STEPS` を 8 → 1(構造ごとの単色。切り替え 3 回/面)、(4) 影を消す、(5) ラベルを目盛り 4 → 2(両端)へ。格子の分割数と点数は削らない(格子は参考の最小値、点数は unit #3 の契約)。いずれも受け入れ基準の数値(4.5・9.5・3.3)に触れるため、削る判断は人間に委ね、値の変更は spec の更新として行う。
- **既定ピッチの反転**: §3 前提 9。`project.ts` の定数 1 箇所の変更で、`orbit.ts` は import 経由で追従する。
- **`axes.ts` の置き換え**: `AXIS_SEGMENTS` を消し、`Vec3` をパネルのモジュールへ移す。`project.ts` の `import type { Vec3 }` の参照先が変わる。ファイル名の扱い(`axes.ts` を改名するか、新設して削除するか)は dev-decompose に委ねる。
- **参考画像**: 中継役から渡された参考画像 3 枚(request.md §2.2 の参考サイト)。WebGL の実装であり(#2 spec `research.md` §1)、同じ見た目への到達を完了条件にしない(roadmap §3)。
- 用語集(`docs/glossary.md`)はリポジトリに無い。名前は既存コードの語彙に合わせた。

## 9. 参考資料

- `docs/specs/002-visual-refinement/004-scatter3d-panes/research.md`(参考画像の読み取り・既存コードの事実・読み替え)
- `docs/specs/002-visual-refinement/request.md` §2.2・§3・§4・§5・§6
- `docs/specs/002-visual-refinement/roadmap.md` §1・§1.1・§2・§3(unit #4 の範囲・完了条件・置き換える契約)
- `docs/specs/001-dashboard-mvp/002-scatter3d-panel/spec.md`(凍結済み。引き継ぐ契約)
- `docs/specs/002-visual-refinement/002-scatter3d-controls/spec.md`・`research.md`(完了済み。置き換える軸線の契約、参考サイトの調査)
- `docs/specs/002-visual-refinement/003-scatter3d-pointcloud/spec.md`・`tasks.md` Implementation Notes(完了済み。引き継ぐ描画規則、完了時の計測、申し送り)
- `docs/specs/001-dashboard-mvp/005-framestats-runtime/tasks.md` B 節(凍結済み。p95 の計測手順)
- Canvas 2D `setTransform`・`drawImage`(変換行列と画像の描画): https://html.spec.whatwg.org/multipage/canvas.html#transformations 、 https://html.spec.whatwg.org/multipage/canvas.html#drawing-images
- `CLAUDE.md`(言語規約・Go / TypeScript コーディング規約・注意事項)
